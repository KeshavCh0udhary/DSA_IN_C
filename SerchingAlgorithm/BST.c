#include<stdio.h>

typedef struct node {
     int data;
     struct node *left;
     struct node *right;

}BST;

BST* root  = NULL;

BST* createNode(BST *root,int data){
    BST* newNode = malloc(sizeof(BST));
    
}


int main(){

    return 0;
}